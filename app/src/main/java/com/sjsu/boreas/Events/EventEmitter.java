package com.sjsu.boreas.Events;

import java.util.HashMap;
import java.util.List;

public interface EventEmitter {
    // interface is empty for now. But a good practice to have all the emitters implement it still.
    // a good way to track all the emitters
}
